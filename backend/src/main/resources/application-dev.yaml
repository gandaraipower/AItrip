# 개발 환경 설정
# 실행: ./gradlew bootRun --args='--spring.profiles.active=dev'
#
# BaseInitData가 자동으로 테스트 계정과 샘플 장소를 생성합니다.
# 테스트 계정: user1@test.com / Test1234!

spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
